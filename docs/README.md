### Documentation made with Sphinx

Setup
```bash
$ pip install sphinx
```

Then, documentation can be built using the `Makefile`
```bash
$ make html
```

Available targets can be shown with `make help`
